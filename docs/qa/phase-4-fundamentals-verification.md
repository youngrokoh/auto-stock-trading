# 4단계 기업 재무 데이터 검증

- 상태: 검증 완료 (수집·저장·읽기 API·지표 계산·가치지표·수급·공시 연결·기업 분석 화면 범위)
- 검증일: 2026-08-17
- 관련 계약: [재무제표 데이터 계약](../data/financial-statement-data-contract.md), [재무 지표 정의 계약](../data/financial-indicator-contract.md), [수급·공시 연결 데이터 계약](../data/investor-flow-disclosure-contract.md)
- 관련 API: [기업 재무 읽기 API](../api/fundamentals-api.md)

## 실제 수집 검증 (2026-08-17)

사용자의 OpenDART 키(`.secrets/dart-api-key`)로 삼성전자(`00126380`)를 수집했다.

| 항목 | 값 |
|---|---|
| 1차 수집 | 보고서 14건 저장(2021~2025 사업보고서, 2026 1분기·반기 × 연결·개별), 2026 3분기 2건 미제출 건너뜀 |
| 멱등 재수집 | 동일 결과(14 저장·2 건너뜀), 전 보고서 `version=1` 유지, 라인 수 불변 |
| 원문 대조 | 자산총계 — 2025 연결 사업보고서 566,942,110,000,000 / 2026 반기 연결 759,480,516,000,000(접수 20260814003699) / 2026 1분기 개별 404,509,604,000,000 — DART 원문 응답과 일치 |
| 라인 수 | 2025 연결 229행, 2026 반기 연결 223행, 2026 1분기 개별 137행 등 원문 응답과 일치 |
| 읽기 API | 보고서 목록 14건, 2025 연결 사업보고서 229라인(자산총계·매출 원문 값), 정정 이력, 미등록 종목 404 확인 |

## 실측으로 정정된 계약 항목 (사용자 승인)

- 자본변동표(SCE)는 같은 원문 순서(`ord`)를 자본 구성요소별 여러 행이 공유한다(예: 2021 사업보고서 `기말자본` ord=13 반복). 라인 유일키를 응답 순번 `(report_id, line_seq)`로 정정했다.
- `account_id` 실측 최대 길이 180자 — 컬럼 폭을 `VARCHAR(255)`로 정정했다.

## 재무 지표 계산 검증 (2026-08-17)

승인된 [재무 지표 정의 계약](../data/financial-indicator-contract.md)에 따라 저장된 삼성전자
연간 보고서(연결, 2021~2025)에서 지표를 계산하고 원문 금액으로 수동 재계산해 대조했다.

| 항목 | 값 |
|---|---|
| 계산 범위 | 연결 5개년 × 지표 9종 전부 값 생성 (성장성 3, 수익성 4, 안정성 2) |
| 2025 수동 대조 | 매출액증가율 (333,605,938−300,870,903)÷300,870,903×100 = 10.88% / 영업이익률 43,601,051÷333,605,938×100 = 13.07% / 부채비율 130,621,773÷436,320,337×100 = 29.94% — API 응답과 일치 |
| 공개 실적 대조 | 2024 매출 300.87조·영업이익 32.73조, 2023 매출 258.94조·영업이익 6.57조 등 원문 값이 공시 실적과 일치, 2024 영업이익증가율 398.34% |
| 개별(OFS) fail-closed | 지배주주 계정이 없어 ROE가 값 없이 `MISSING_ACCOUNT` 반환, 입력 금액은 전부 `null` |
| HTTP 오류 계약 | 미등록 종목 404, `fs_div=bogus` 422 확인 |
| 출처 노출 | 연도별 `rcept_no`·`version`, 지표별 수식·입력 계정·금액이 응답에 포함 |

## 기업 분석 화면 검증 (2026-08-17)

승인 디자인 2b(A 지표 KPI 7 / B 실적 차트·재무제표 / C 지표·가치 / D 수급·공시)를 승인
토큰·프리미티브로 구현하고 실제 브라우저로 확인했다.

- e2e 12건(운영 개요·시장 데이터·기업 분석·갤러리 × 모바일 390·태블릿 768·데스크톱 1360)
  통과: 콘솔 오류 0, 가로 넘침 없음, 연결 URL 미노출, 손익계산서 탭 전환, 개별 전환 시
  ROE "지배주주 계정 없음" 표시.
- 세 폭과 다크 모드 전체 페이지 스크린샷을 육안 확인: 태블릿 접기 규칙(레일 유지·내비
  숨김·1열·KPI 3열) 유지, 7번째 KPI는 태블릿·모바일에서 전체 폭으로 접힘, 재무제표 구분
  탭 줄바꿈 정상, 다크 토큰 일치.
- 백엔드가 없는 가치 지표·수급·공시 연결은 값 생성 없이 사유를 적은 빈 상태 카드다.
- 재무제표 카드의 금액은 DART 원문 라인 그대로이며(자산총계 566,942,110,000,000 표시
  확인) KPI의 조 단위 축약은 표시 전용이다.

## 상장주식수·가치지표 검증 (2026-08-17)

계약 정정(가치지표 승인분)에 따라 상장주식수 수집과 `valuation` 블록을 검증했다.

| 항목 | 값 |
|---|---|
| 실수집 | KIS 모의환경 시세 수집으로 삼성전자 5,846,278,608주(v1)·KODEX 200 236,250,000좌(v1) 적재, `as_of`는 시세 수신 시각 |
| 멱등 재수집 | 같은 값 재관측 후에도 종목별 1행·`version=1` 유지 |
| 버전 규칙 | 통합 테스트에서 값 변경 관측이 이전 버전 보존(`superseded_at` 기록) 후 `version=2` 생성 확인 |
| 가치지표 수동 대조 | 연결 기본주당이익 6,605원(공시 원문) / PER 274,500 ÷ 6,605 = 41.56배 / 시가총액 274,500 × 5,846,278,608 = 1,604,803,477,896,000원 — API 응답과 일치. 개별 기준 5,027원·54.61배 |
| 기준 노출 | 응답이 가격 기준시각(2026-08-17 시세)·재무 기준(`rcept_no` 20260310002820)·주식수 기준을 별도 명시 |
| 파생 EPS 미사용 | 지배주주순이익 ÷ 보통주 주식수 = 7,570.79원은 우선주 미반영 왜곡이므로 계산하지 않음(계약 기록) |
| ETF | 연간 보고서가 없어 `valuation=null` |
| 화면 | C2 가치 카드가 실값(6,605원·41.56배·1,604.8조원)과 기준 3종을 표시, BPS·PBR은 후속 사유 노출. e2e 12건·3폭·다크 재확인 |

## 수급·공시 연결 검증 (2026-08-17)

승인된 [수급·공시 연결 데이터 계약](../data/investor-flow-disclosure-contract.md)에 따라
실수집·멱등·화면을 검증했다.

| 항목 | 값 |
|---|---|
| 수급 실수집 | KIS 모의환경에서 삼성전자·KODEX 200 각 30거래일(2026-07-03~08-14) 적재, 서울 기준 당일(8/17) 행 없음 |
| 수급 멱등·버전 | 재수집 후 60행·전부 `version=1` 유지. 통합 테스트에서 값 변경 관측이 이전 버전 보존 후 `version=2` 생성 확인 |
| 수급 원문 대조 | 8/14 삼성전자 외국인 +4,913,433주·1,336,152백만원 등 KIS 원본 응답 값과 일치 |
| 공시 실수집 | 최근 1년 2,837건(정기 4·주요사항 9·지분 2,758·거래소 66), 재수집 후 행 수 불변 |
| 읽기 API | 수급 정렬·단위(`share`/`million_krw`)·404, 공시 정렬(접수일·접수번호 내림차순)·유형·404 확인 |
| 화면 | D2 수급 표(최근 8거래일, 순매수 부호 색상)와 D3 공시 목록(유형·제출인·DART 원문 링크) 실값 표시, e2e 12건·3폭 재확인 |

## 유니버스 재무제표 수집 검증 (2026-08-21)

승인 개정된 [재무제표 데이터 계약](../data/financial-statement-data-contract.md) §유니버스 수집 규칙에 따라
KOSPI 200 전 종목을 실제 OpenDART 키로 수집했다.

| 항목 | 값 |
|---|---|
| 실행 | `worker/fundamentals.py --universe-statements`, 14:46:46~14:55:48 KST(9분 2초) |
| 결과 | `symbols=200 saved=2646 skipped=554 existing=0 failed=0 missing_corp_codes=0 quota_exhausted=False` |
| 요청 수 | 2,646 + 554 = 3,200 = 200종목 × 8기간 × 2연결구분. 계약 §유니버스 2의 예상치와 일치, 일 한도 20,000의 16% |
| 저장량 | 보고서 2,646건 / 라인 515,144건 / 전부 `version=1` |
| 멱등성 | 기존 삼성전자 14건은 같은 접수번호라 버전 불변, 2025 연결 자산총계 566,942,110,000,000이 이전 검증값과 동일 |
| 미제출 554건 | 2026 3분기 400건(전 종목) + 5개년 미달 신규 상장 150건 + 연결·개별 한쪽만 존재 4건(카카오뱅크 연결 없음, 두산밥캣 개별 없음) |
| 정합성 독립 검증 | 현재 버전 2,646건 중 2,637건에서 `자산총계 = 부채총계 + 자본총계` 정확히 성립. 예외는 표준 계정 미태깅 6건과 원문 1,000원 반올림 3건(원문 보존) |
| 상태 기록 | 종목 200건 전부 `success`, 스윕 행(`universe_financial_statements` / `KOSPI200`)도 `success` |
| 읽기 API | 신규 종목(000660·005380·035420·000270) 보고서 목록 14건과 접수번호·버전 정상 응답 |

측정된 원천 특성 두 가지는 계약에 기록했고 데이터를 고치지 않는다: 손익 계정이 회사에 따라
`IS`(51건) 또는 `CIS`(147건)에 실린다는 점, 연결·개별 중 한쪽만 제출하는 회사가 있다는 점.

### 이 검증이 드러낸 지표 한계

[재무 지표 정의 계약](../data/financial-indicator-contract.md)은 손익 입력을 `sj_div = IS`로 고정한다.
2025 연결 사업보고서 198건 중 `IS`를 제출한 곳은 51건뿐이므로, 나머지 147종목의 성장성·수익성
지표가 모두 `MISSING_ACCOUNT`로 응답한다(SK하이닉스 실측 확인). 안정성 지표(부채비율·자기자본비율)는
`BS` 기준이라 영향이 없다. `IS`와 `CIS`를 함께 읽으면 매출액 180/198, 영업이익 179/198,
당기순이익 198/198, 지배주주순이익 194/198, 기본주당이익 182/198로 늘어난다. 승인 계약을 구현
편의로 바꾸지 않으므로 이 항목은 사용자 결정 대기 상태다.

## 자동 검증 명령

```bash
cd backend
uv run pytest tests/fundamentals tests/disclosures/test_opendart_financials.py \
  tests/api/test_fundamentals_api.py tests/worker/test_fundamentals.py
cd frontend
bun run test    # 지표 스키마·조/억 포맷 단위 테스트 포함
bun run e2e     # API :8000 + Vite :5173 필요
```

fixture·PostgreSQL 통합 테스트가 멱등 재수집, 접수번호 기반 정정 버전과 오래된 접수번호 거부, 미제출(013) 건너뜀, sync 상태 기록, 읽기 API 계약을 검증한다. 지표 단위 테스트는 고정 입력의 수치 벡터와 fail-closed 사유 4종(계정 없음·중복·금액 없음·분모 0)을 검증한다.

## 남은 범위

- BPS·PBR — 우선주(종목·주식수·자본 배분) 반영 설계 확정 후
- 손익 지표의 `CIS` 반영 여부 — 사용자 결정 대기(위 항목 참고)
